<!DOCTYPE html>
<html>
<head>
  <base target="_top">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OPERATOR - Account Open</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }
    .container {
      max-width: 700px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
    }
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 30px;
      text-align: center;
    }
    .header h1 { font-size: 28px; margin-bottom: 10px; }
    .badge {
      background: #ff9800;
      color: white;
      padding: 8px 20px;
      border-radius: 25px;
      font-size: 13px;
      font-weight: bold;
      margin-top: 10px;
      display: inline-block;
    }
    .content { padding: 35px; }
    .alert {
      padding: 18px;
      border-radius: 12px;
      margin-bottom: 25px;
      border-left: 5px solid #ff9800;
      background: #fff3cd;
      color: #856404;
    }
    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-bottom: 25px;
    }
    .form-group { margin-bottom: 25px; }
    .form-group label {
      display: block;
      margin-bottom: 10px;
      font-weight: 600;
      color: #333;
    }
    .form-group input, .form-group select {
      width: 100%;
      padding: 14px;
      border: 2px solid #e0e0e0;
      border-radius: 10px;
      font-size: 15px;
    }
    .form-group input:focus, .form-group select:focus {
      border-color: #667eea;
      outline: none;
    }
    .form-group input[readonly] { background: #f8f9fa; color: #6c757d; }
    .required::after { content: ' *'; color: #dc3545; }
    .btn {
      width: 100%;
      padding: 16px;
      border: none;
      border-radius: 12px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      margin-top: 10px;
    }
    .btn:hover { transform: translateY(-2px); }
    .btn:disabled { background: #ccc; cursor: not-allowed; }
    .message {
      padding: 15px;
      border-radius: 10px;
      margin-bottom: 20px;
      display: none;
    }
    .error { background: #f8d7da; color: #721c24; }
    .success { background: #d4edda; color: #155724; }
    @media (max-width: 600px) {
      .form-row { grid-template-columns: 1fr; }
    }
    @keyframes shake {
  0%, 100% { transform: translateX(-50%) translateY(0); }
  25% { transform: translateX(-50%) translateY(-5px); }
  75% { transform: translateX(-50%) translateY(5px); }
}
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 id="headerTitle">🏦 Account Open</h1>
      <div class="badge">🔒 OPERATOR MODE - PENDING APPROVAL</div>
    </div>

    <div class="content">
      <div class="alert">
        <strong>⚠️ IMPORTANT:</strong> This account will be submitted for supervisor approval before activation.
      </div>

      <form id="accountForm">
        <div class="form-row">
          <div class="form-group">
            <label class="required">🆔 Member ID</label>
            <select id="memberId" required>
              <option value="">-- Select Member --</option>
            </select>
          </div>
          
          <div class="form-group">
            <label>📋 Account Number</label>
            <input type="text" id="accountNo" readonly>
          </div>
        </div>

        <div class="form-group">
          <label>👤 Member Name</label>
          <input type="text" id="memberName" readonly>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label class="required">📅 Opening Date</label>
            <input type="date" id="openDate" required readonly style="background:#f8f9fa;color:#6c757d;cursor:not-allowed;">
          </div>
          
          <div class="form-group">
            <label class="required">🎂 Date of Birth</label>
            <input type="date" id="dob" required>
          </div>
        </div>

        <div class="form-group">
          <label class="required">💼 Account Type</label>
          <select id="accountType" required>
            <option value="">-- Select Type --</option>
            <option value="SIP">SIP</option>
            <option value="LUMPSUM">LUMPSUM</option>
            <option value="FIXED">FIXED</option>
            <option value="SAVINGS">SAVINGS</option>
          </select>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label class="required">👥 Nominee Name</label>
            <input type="text" id="nomineeName" required placeholder="Enter nominee name">
          </div>
          
          <div class="form-group">
            <label class="required">🔗 Relationship</label>
            <input type="text" id="relationship" required placeholder="e.g., Father, Mother">
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label class="required">Photo URL</label>
            <input type="url" id="photoUrl" placeholder="https://drive.google.com/...">
            <div class="helper-text">Google Drive link</div>
          </div>
           </div> 

        <div class="form-group">
          <label>📧 Email</label>
          <input type="email" id="email" readonly>
        </div>

        <div class="form-group">
          <label>🆔 User ID</label>
          <input type="text" id="userId" readonly>
        </div>

        <div class="form-group">
          <label class="required">📊 Status</label>
          <select id="status" required>
            <option value="ACTIVE">ACTIVE</option>
            <option value="INACTIVE">INACTIVE</option>
          </select>
        </div>

        <div id="errorMsg" class="message error"></div>
        <div id="successMsg" class="message success"></div>

        <button type="submit" class="btn" id="submitBtn">
                📤 Submit for Supervisor Approval
        </button>
      </form>
    </div>
  </div>

  <script>
    // ✅ FIXED INIT: Single clean load event — no race conditions, no dead code
    let operatorId    = null;
    let operatorToken = null;
    let userBranchCode = '';

    window.addEventListener('load', function() {
      // Step 1: Get operatorId — URL param first, then window inject fallback
      operatorId    = new URLSearchParams(window.location.search).get('operator') || (window.OPERATOR_ID || null);
      operatorToken = (new URLSearchParams(window.location.search).get('token'))
                      ? decodeURIComponent(new URLSearchParams(window.location.search).get('token'))
                      : (window.OPERATOR_TOKEN || null);

      if (!operatorId) {
        alert('❌ Operator ID not found!\n\nPlease close this window and login again.');
        window.close();
        return;
      }

      // Step 2: Show operator ID in header
      const header = document.getElementById('headerTitle');
      if (header) {
        header.innerHTML += '<br><small style="font-size:14px;opacity:0.9;">Operator: ' + operatorId + '</small>';
      }

      // Step 3: Fetch branch code + show branch banner
      google.script.run
        .withSuccessHandler(function(result) {
          if (result && result.success) {
            userBranchCode = result.branchCode || '';
            const bannerDiv = document.createElement('div');
            bannerDiv.style.cssText = 'background:linear-gradient(135deg,#667eea,#764ba2);color:white;padding:8px 20px;text-align:center;font-size:13px;';
            bannerDiv.innerHTML = '<strong>Operator:</strong> ' + operatorId + ' &nbsp;|&nbsp; <strong>Branch:</strong> ' + userBranchCode;
            const container = document.querySelector('.container') || document.body;
            container.insertBefore(bannerDiv, container.firstChild);
          }
        })
        .getOperatorDetails(operatorId);

      // Step 4: Load form data
      initFormData();
    });

    function initFormData() {
      loadMembers();
      loadNextAccountNumber();
    }

    document.getElementById('openDate').valueAsDate = new Date();
      document.getElementById('openDate').readOnly = true; // ✅ No backdate



    function loadMembers() {
      google.script.run
        .withSuccessHandler(function(res) {
          if (res.success) {
            const select = document.getElementById('memberId');
            res.members.forEach(member => {
              const option = document.createElement('option');
              option.value = member.memberId;
              option.textContent = member.memberId + ' - ' + member.name;
              option.dataset.name = member.name;
              option.dataset.email = member.email;
              option.dataset.userId = member.userId;
              select.appendChild(option);
            });
          }
        })
        .getMembersForDropdown(operatorId);
    }

    function loadNextAccountNumber() {
      google.script.run
        .withSuccessHandler(function(res) {
          if (res.success) {
            document.getElementById('accountNo').value = res.nextAccountNo;
          }
        })
        .getNextAccountNumber();
    }

    document.getElementById('memberId').addEventListener('change', function() {
      const selected = this.options[this.selectedIndex];
      if (selected.value) {
        document.getElementById('memberName').value = selected.dataset.name || '';
        document.getElementById('email').value = selected.dataset.email || '';
        document.getElementById('userId').value = selected.dataset.userId || '';
      }
    });

    document.getElementById('accountForm').addEventListener('submit', function(e) {
      e.preventDefault();
      submitAccount();
    });

    function submitAccount() {
  const btn = document.getElementById('submitBtn');
  const errorDiv = document.getElementById('errorMsg');
  const successDiv = document.getElementById('successMsg');

  errorDiv.style.display = 'none';
  successDiv.style.display = 'none';

  if (!operatorId) {
    showError('❌ Operator ID missing!');
    return;
  }

  const accountData = {
    memberId:      document.getElementById('memberId').value.trim(),
    name:          document.getElementById('memberName').value.trim(),
    accountNo:     document.getElementById('accountNo').value.trim(),     // ✅ FIXED: accountNo add kiya
    openDate:      document.getElementById('openDate').value,
    accountOpenDate: document.getElementById('openDate').value,           // ✅ Both formats
    dob:           document.getElementById('dob').value,
    accountType:   document.getElementById('accountType').value,
    nomineeName:   document.getElementById('nomineeName').value.trim(),
    relationship:  document.getElementById('relationship').value.trim(),
    email:         document.getElementById('email').value.trim(),
    userId:        document.getElementById('userId').value.trim(),
    status:        document.getElementById('status').value,
    photoUrl:      document.getElementById('photoUrl').value.trim(),
    branchCode:    userBranchCode
  };

  if (!accountData.memberId || !accountData.accountNo || !accountData.accountType) {
    showError('❌ Please fill all required fields');
    return;
  }

  btn.disabled = true;
  btn.textContent = '⏳ Submitting...';

  console.log('📤 Submitting account | Operator:', operatorId, '| Account:', accountData.accountNo);

  google.script.run
    .withSuccessHandler(function(res) {
      btn.disabled = false;
      btn.textContent = '📤 Submit for Supervisor Approval';

      if (res.success) {
        showSuccess(`✅ ${res.message}<br>Entry ID: ${res.entryId}<br>Operator: ${operatorId}`);
        document.getElementById('accountForm').reset();
        document.getElementById('openDate').valueAsDate = new Date();
      document.getElementById('openDate').readOnly = true; // ✅ No backdate
        loadNextAccountNumber();
      } else {
        showError('❌ ' + res.message);
      }
    })
    .withFailureHandler(function(err) {
      btn.disabled = false;
      btn.textContent = '📤 Submit for Supervisor Approval';
      showError('❌ Error: ' + err.message);
    })
    // ✅ FIXED: operatorId PEHLE, accountData BAAD MEIN
    .operatorSubmitAccountOpen(operatorId, accountData, operatorToken);
}

    function showError(msg) {
      document.getElementById('errorMsg').innerHTML = msg;
      document.getElementById('errorMsg').style.display = 'block';
      document.getElementById('successMsg').style.display = 'none';
    }

    function showSuccess(msg) {
      document.getElementById('successMsg').innerHTML = msg;
      document.getElementById('successMsg').style.display = 'block';
      document.getElementById('errorMsg').style.display = 'none';
    }
  </script>
</body>
</html>
